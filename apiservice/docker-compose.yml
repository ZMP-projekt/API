version: '3.8'

services:
  gym-api:
    build: .
    container_name: gym-api-service
    ports:
      - "8080:8080"
   environment:
      - DB_URL=${DB_URL}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - SPRING_DOCKER_COMPOSE_ENABLED=false
    restart: always